{
    "name": "{{appName}}",
    "version": "0.0.0",
    "scripts": {
        "start": "node ./dist/index.js",
        "dev": "tsc -w",
        "build": "tsc -p ."
    },
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^{{nodeTypesVersion}}",
        "typescript": "^{{typescriptVersion}}"
    }
}
